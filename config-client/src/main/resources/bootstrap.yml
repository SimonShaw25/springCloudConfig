spring:
  application:
    name: configClient                     #指定了配置文件的应用名
  cloud:
    config:
      uri: http://localhost:8888/        #Config server的uri
      profile: dev                       #指定的环境
      label: master
server:
  port: 8881